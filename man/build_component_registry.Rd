% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_component_registry.R
\name{build_component_registry}
\alias{build_component_registry}
\title{Build a component registry from a causal theory specification}
\usage{
build_component_registry(
  nodes,
  timing = NULL,
  descriptions = NULL,
  respect_timing = TRUE,
  include_bidirectional = FALSE,
  constraints = NULL,
  exposure = NULL,
  outcome = NULL,
  time_points = NULL,
  timing_options = NULL,
  optional_nodes = character(),
  url = getOption("theoRy.engine_url", "http://localhost:8000"),
  output_path = NULL
)
}
\arguments{
\item{nodes}{A character vector of variable names, or a data frame with
columns \code{name}, \code{timing} (integer >= 1), and optionally
\code{description}.}

\item{timing}{Integer vector of fixed chronological positions (all values
>= 1) parallel to \code{nodes}. Use \code{NA} for a non-focal node whose
possible positions are supplied by \code{time_points}. Ignored when
\code{nodes} is a data frame.}

\item{descriptions}{Optional character vector of descriptions parallel to
\code{nodes}. Ignored when \code{nodes} is a data frame.}

\item{respect_timing}{When \code{TRUE} (default), directed candidates must
be temporally possible.}

\item{include_bidirectional}{When \code{TRUE}, generate candidate
bidirected edges for unordered node pairs that can occupy the same time
when \code{respect_timing = TRUE}. With flexible timing, the nodes must
share at least one timing option. Prefer explicit \code{<->}
\code{"allow"} constraints for selected possible confounding, including
cross-time pairs, or \code{"require"} constraints to enforce named
confounding pairs. When \code{respect_timing = FALSE}, every unordered
node pair is eligible.}

\item{constraints}{Optional data frame or list with \code{source},
\code{target}, \code{direction}, and \code{rule}. Directed
and bidirected \code{"require"} edges are fixed causal paths or
confounding pairs, \code{"forbid"} removes a path or pair, and
\code{"allow"} permits a selected candidate.}

\item{exposure}{Name of the focal exposure. Must be a node with exactly one
allowed time.}

\item{outcome}{Name of the focal outcome. Must be a node with exactly one
allowed time later than \code{exposure}.}

\item{time_points}{Finite integer vector of positions >= 1 available to
non-focal nodes whose \code{timing} is \code{NA}. Required whenever timing
is unspecified.}

\item{timing_options}{Optional named list of allowed integer positions (all
values >= 1). It overrides \code{timing} for named nodes. Each model
chooses one position from each present node's options.}

\item{optional_nodes}{Node names that may be absent in subset models.
Exposure, outcome, and endpoints of required paths cannot be optional.}

\item{url}{Base URL of the theoRy Python backend.}

\item{output_path}{Optional Parquet path for the registry table. Timing and
theory metadata are R attributes and are not retained by Parquet.}
}
\value{
A registry data frame. It retains \code{exposure}, \code{outcome},
  \code{node_timing}, \code{timing_options}, \code{optional_nodes}, and
  \code{constraints} attributes for downstream expansion.
}
\description{
Builds a registry programmatically from named variables and their positive
temporal positions (integer values >= 1). An exposure and outcome are always
required; their directed path is fixed as causal in every generated model. Use
\code{build_component_registry_interactive()} for a guided alternative.
}
\examples{
\dontrun{
start_theory_engine()
registry <- build_component_registry(
  nodes = c("Education", "Income", "Health"),
  timing = c(1, 2, 3),
  exposure = "Education",
  outcome = "Health"
)
}

}
