% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_symbolic_multiverse.R
\name{build_symbolic_multiverse}
\alias{build_symbolic_multiverse}
\title{Build a Symbolic Multiverse}
\usage{
build_symbolic_multiverse(
  nodes = NULL,
  timing = NULL,
  registry = NULL,
  exposure = NULL,
  outcome = NULL,
  preferred_model = NULL,
  constraints = NULL,
  mode = c("full", "sampled"),
  n_samples = 5000L,
  url = getOption("theoRy.engine_url", "http://localhost:8000")
)
}
\arguments{
\item{nodes}{A data frame or list of nodes with name and optional timing
columns containing integer values >= 1; \code{NA} means unspecified.}

\item{timing}{An optional named list/vector of integer timing values >= 1;
\code{NA} means unspecified.}

\item{registry}{An optional registry data frame (from build_component_registry).}

\item{exposure}{Name of the exposure variable.}

\item{outcome}{Name of the outcome variable.}

\item{preferred_model}{An optional DAG spec list with nodes, edges, exposure, outcome.}

\item{constraints}{Optional edge constraints list.}

\item{mode}{Symbolic mode: "full" or "sampled".}

\item{n_samples}{Number of samples for sampled mode.}

\item{url}{Base URL for the theory engine API.}
}
\value{
A list with class "theory_symbolic_multiverse".
}
\description{
Constructs a symbolic multiverse either from a component registry
(\code{registry}) or directly from nodes and positive timing positions
(integer values >= 1). When no timing is
supplied and both \code{exposure} and \code{outcome} are provided, an
implicit \code{exposure -> outcome} edge variable is created and fixed as
causal in the symbolic universe, enforcing the causal ordering constraint
in every symbolic query.
}
