% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_model_states.R
\name{expand_model_states}
\alias{expand_model_states}
\title{Expand a component registry into model-state records}
\usage{
expand_model_states(
  registry,
  mode = c("seeded", "exhaustive", "sampled"),
  seed_claims = NULL,
  node_timing = NULL,
  max_models = 10000L,
  n_models = NULL,
  seed = NULL,
   edge_statuses = c("causal", "unknown", "non-causal"),
  exposure = NULL,
  outcome = NULL,
  url = getOption("theoRy.engine_url", "http://localhost:8000")
)
}
\arguments{
\item{registry}{A registry data frame or path to a Parquet file.}

\item{mode}{Expansion mode: \code{"seeded"}, \code{"exhaustive"}, or \code{"sampled"}.}

\item{seed_claims}{A data frame or list of state claims with
\code{model_id}, \code{comp_id}, \code{status}, and optionally
\code{timing}. Required for \code{mode = "seeded"}.}

\item{node_timing}{Optional named integer vector for temporal validation.}

\item{max_models}{Safety cap for exhaustive mode (default 10,000).}

\item{n_models}{Number of models to sample in \code{"sampled"} mode.}

\item{seed}{Random seed for reproducible sampling.}

\item{edge_statuses}{Character vector of edge statuses to enumerate
or sample over in exhaustive and sampled modes.
Defaults to \code{c("causal", "unknown", "non-causal")}.
Pass \code{c("causal", "unknown")} for binary (old) behavior.}

\item{exposure}{Optional exposure variable name to attach as state metadata.
If omitted, registry exposure metadata is inherited when available.}

\item{outcome}{Optional outcome variable name to attach as state metadata.
If omitted, registry outcome metadata is inherited when available.}

\item{url}{Base URL of the theoRy Python backend.}
}
\value{
A data frame with columns: \code{model_id}, \code{comp_id},
\code{status}, \code{timing}. Optional \code{exposure} and \code{outcome}
attributes are attached when provided or inherited from the registry.
}
\description{
Takes a component registry and generates model-state records using
seeded, exhaustive, or sampled expansion.
}
\details{
\describe{
  \item{seeded}{Fill user-provided model claims; default unspecified
     components to \code{"unknown"}.}
   \item{exhaustive}{Enumerate all valid combinations of edge statuses
     (by default \code{"causal"}, \code{"unknown"}, \code{"non-causal"})
     under temporal and DAG constraints.
     Only \code{"causal"} edges are subject to temporal/DAG validation.}
  \item{sampled}{Generate a random sample of valid model states from
    the same edge-status space.}
}
}
\examples{
\dontrun{
start_theory_engine()

reg <- build_component_registry(c("X", "Y", "Z"), c(1, 2, 3))

# Exhaustive tri-state (default): causal, unknown, non-causal
states <- expand_model_states(reg, mode = "exhaustive",
  node_timing = c(X = 1, Y = 2, Z = 3))
head(states)

# Binary (old behavior): causal, unknown only
states2 <- expand_model_states(reg, mode = "exhaustive",
  node_timing = c(X = 1, Y = 2, Z = 3),
   edge_statuses = c("causal", "unknown"))
head(states2)
}
}
