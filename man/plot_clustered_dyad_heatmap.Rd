% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_clustered_dyad_heatmap.R
\name{plot_clustered_dyad_heatmap}
\alias{plot_clustered_dyad_heatmap}
\title{Plot a cluster-sorted dyad heatmap}
\usage{
plot_clustered_dyad_heatmap(
  result = NULL,
  dyads = NULL,
  cluster_assignments = NULL,
  cluster_labels = NULL,
  score_field = "similarity_rate",
  max_models = 200L,
  show_labels = FALSE,
  ...
)
}
\arguments{
\item{result}{Optional simulation result from
\code{run_simulation("ghost_discovery")} created with
\code{include_plot_data = TRUE}.  Uses
\code{result$artifacts$plot_data$dyad_heatmap}.}

\item{dyads}{Optional dyad data frame.  Must contain \code{ego_id},
\code{alter_id}, and a score column.  Used when \code{result} is
not supplied.}

\item{cluster_assignments}{Optional data frame with \code{model_id} and
\code{cluster_id} columns.  Required when \code{dyads} is supplied
without \code{result}.}

\item{cluster_labels}{Optional data frame with \code{cluster_id} and
\code{label} columns.}

\item{score_field}{Character.  Column name for the heatmap color
score.  Default \code{"similarity_rate"}; simulation plot data may
also supply a precomputed \code{"score"} column.}

\item{max_models}{Maximum number of models to display (default 200).}

\item{show_labels}{Logical.  Whether to show model ID labels on axes.
Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2]{ggplot}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a dyad similarity heatmap with models ordered by cluster
assignment, revealing block structure that validates detected clusters.
Ghost, mainstream, fragmented, and noise clusters are visually
separated.
}
\examples{
\dontrun{
start_theory_engine()
ghost <- run_simulation("ghost_discovery",
  n_models = 150, include_plot_data = TRUE)
plot_clustered_dyad_heatmap(ghost)

dyads <- build_dyad_matrix(reg, states, mode = "basic")
clusters <- detect_ghost_clusters(dyads, prior_model = "M0001")
plot_clustered_dyad_heatmap(
  dyads = dyads,
  cluster_assignments = clusters$cluster_assignments
)
}

}
